import { NewMovementClient } from './NewMovementClient';

export const metadata = { title: 'Nouveau mouvement — StockFlow' };

export default function NewMovementPage() {
  return <NewMovementClient />;
}